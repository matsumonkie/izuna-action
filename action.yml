name: 'izuna-action'
description: 'Run izuna'
inputs:
  packageName:
    description: 'Name of the package'
    required: true
  ghcVersion:
    description: 'Version of Ghc used'
    required: true
  hieDirectory:
    description: 'Where the hie files are stored'
    required: true
    default: '.hie'
runs:
  using: 'node12'
  main: 'index.js'
